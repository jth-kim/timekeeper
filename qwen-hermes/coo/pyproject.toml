[project]
name = "timekeeper-coo-hermes"
version = "0.1.0"
description = "Timekeeper COO — hermes-style agent architecture"
requires-python = ">=3.12"

dependencies = [
    "httpx>=0.27",
    "tomli>=2.0",
    "structlog>=24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
